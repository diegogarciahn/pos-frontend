
import 'package:flutter/material.dart';

class Mantenimiento extends StatelessWidget {
   
  const Mantenimiento({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('Mantenimiento'),
      ),
    );
  }
}